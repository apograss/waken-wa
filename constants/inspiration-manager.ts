/** Server page size; keep small so tall markdown cards do not overflow the viewport. */
export const INSPIRATION_LIST_PAGE_SIZE = 8
/** Max scroll height for the entry list inside the card (viewport-relative). */
export const INSPIRATION_LIST_MAX_HEIGHT = 'min(75vh,56rem)'
/** Max long edge (px) for cropped PNG DataURL (cover + inline body images). */
export const INSPIRATION_MAX_OUTPUT_EDGE = 1200
/** Local draft storage key for admin inspiration form. */
export const INSPIRATION_DRAFT_STORAGE_KEY_V1 = 'waken:admin:inspiration-draft:v1'
export const INSPIRATION_DRAFT_STORAGE_KEY = 'waken:admin:inspiration-draft:v2'
