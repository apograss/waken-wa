# Quick start: ./deploy.sh
# Local stack: all-in-one container (Next.js app + internal Redis + SQLite).
# Schema: Drizzle (`drizzle-kit push` in entrypoint). JWT: optional in .env; if empty, entrypoint persists /app/data/.jwt_secret.
services:
  app:
    image: ${WAKEN_IMAGE:-ghcr.io/moyoez/waken-wa:main}
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-3000}:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL:-file:/app/data/dev.db}
      REDIS_INTERNAL_ENABLED: ${REDIS_INTERNAL_ENABLED:-1}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_URL: ${REDIS_URL:-redis://127.0.0.1:6379}
      REDIS_CACHE_TTL_SECONDS: ${REDIS_CACHE_TTL_SECONDS:-3600}
      HOST: "0.0.0.0"
      HOSTNAME: "0.0.0.0"
      JWT_SECRET: ${JWT_SECRET:-}
      NODE_ENV: production
      NEXT_PUBLIC_BASE_URL: ${NEXT_PUBLIC_BASE_URL:-http://localhost:3000}
      HCAPTCHA_SITE_KEY: ${HCAPTCHA_SITE_KEY:-}
      HCAPTCHA_SECRET_KEY: ${HCAPTCHA_SECRET_KEY:-}
      STEAM_API_KEY: ${STEAM_API_KEY:-}
      MEMOS_WEBHOOK_SECRET: ${MEMOS_WEBHOOK_SECRET:-}
    volumes:
      - waken_sqlite_data:/app/data

volumes:
  waken_sqlite_data:
